fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
SQLAlchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
neo4j==5.24.0
celery==5.4.0
redis==5.0.8
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend self-test is broken against bcrypt>=4.1
# (which dropped the __about__ attribute passlib checks and tightened the
# 72-byte limit) — pin the last known-compatible bcrypt release explicitly.
bcrypt==4.0.1
python-multipart==0.0.9
cryptography==43.0.1
reportlab==4.2.2
httpx==0.27.2
# AI/ML stylometric analysis (app/services/ai_stylometry.py): TF-IDF
# character/word n-grams + cosine similarity. Deliberately NOT a
# sentence-transformers/torch stack — that pulls in ~700MB+ of GPU-oriented
# dependencies and a downloaded model, unnecessary CPU/RAM/image-size cost
# on a t3.small already running Postgres/Neo4j/Redis/API/Celery. scikit-learn
# ships pure CPU wheels, no model download, no network call at runtime.
scikit-learn==1.5.2

pytest==8.3.3
pytest-cov==5.0.0
ruff==0.6.9
